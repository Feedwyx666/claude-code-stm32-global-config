# 用户全局约定（任何目录启动时自动加载）

以下是用户的核心偏好与规则。**无条件规则直接遵守；带"适用条件"的规则只在条件满足时生效。**

## 通用偏好（无条件）

1. **始终用中文回复**。代码、命令、标识符、日志原文保持原样不翻译。
2. **用户在学习嵌入式开发**（重点 STM32）。回答时侧重**解释原理**（为什么、底层机制），不只是给结论。用户提出方案时，先分析这个想法对错、为什么，再谈是否修改。
3. **用户用询问/试探语气提问**（如「...是不是可以」「...是不是就能解决」「能不能...」）时，表示在征询意见、想学习判断，**先解释判断，再问「需要我改吗」**，等确认后才动手。只有明确指令语气（如「改一下」「把X改成Y」）才直接改。
4. **记忆读取（无条件，强制）**：**每次会话开始，都主动读取全局记忆（`%USERPROFILE%\.claude\memory\`）和该项目路径对应的全部项目记忆（`%USERPROFILE%\.claude\projects\<项目名>\memory\`，如有）**。不是只读索引 `MEMORY.md`，而是把索引里指向的每个记忆文件的实际内容都读一遍。读完在回复中说明「已读取记忆」并概括所知道的要点。

## 带适用条件的规则

### 5. 改动前留回退点 —— 强制要求，**每次修改前必做**，不得跳过

> **核心硬性规定：凡是要修改或删除任何文件，动手之前必须先留一个可回退点。** 不允许"忘了"、"之后再补"。

**第一步：先判断当前目录有没有 git 版本控制**（`git rev-parse --git-dir`）

**情况 A：在 git 仓库内 → 用 git 提交代替备份，不做任何文件复制**
- `git status --short` 看工作区状态。
- **有未提交改动** → 先提交存档作为回退点：`git add <相关文件>` + `git commit -m "改动前存档：<简述>"`（逐个列文件，不用 `git add .`）。
- **有远程仓库**（`git remote -v` 非空）→ 再 `git push` 一并同步，让回退点也存在远端。
- **工作区本来就干净** → 无需提交，当前 HEAD 就是回退点，直接改。
- 推送失败不要硬推、不要 `--force`；本地提交已是有效回退点，告知用户失败原因即可。

**情况 B：不在 git 仓库内 → 按目录类型做文件复制备份**
- **工程目录**（含 `.uvprojx`/`.sln`/`package.json`/`CMakeLists.txt` 等工程文件）：首次使用时，在项目**上一级目录**创建 `<项目名>_backup_时间戳\` 完整备份；已有则跳过。之后不再每次备份。
- **普通/文件多目录**（桌面、C 盘/D 盘根目录、Downloads 等，含**可能为空的工程文件夹**）：**不要备份整个目录**（太大了没意义），**只备份你要修改或操作的单个文件**（可复制一份带时间戳的后缀，或放进 `_file_backups` 子目录）。
- **纯临时/测试目录**：不需要备份。
- 备份后立刻核对文件已生成、内容与源文件一致。
- **所有备份最多保留两份**，新的自动覆盖最旧的（超出 2 份就删除最旧一份）。

**第二步：告知用户**
- 无论走 A 还是 B，完成后在回复中**明确告知用户**（一句话即可）：提交了什么 / 是否推送 / 或备份放在哪。
- 用户明确说「不用备份」时以用户为准，不要坚持。

### 6. Keil 编译/烧录 —— **仅当**当前目录是 Keil 工程（有 `.uvprojx`）时适用
- Keil 位于 `C:\Keil_v5\UV4\UV4.exe`。
- 编译：`"/c/Keil_v5/UV4/UV4.exe" -b "Project.uvprojx" -j0 -o "build_log.txt"`
- 烧录：`"/c/Keil_v5/UV4/UV4.exe" -f "Project.uvprojx" -o "flash_log.txt"`
- 成功标志：编译 `0 Error(s), 0 Warning(s)`；烧录 `Verify OK. Application running`。

### 7. 启动文件 `start-claude.bat` 与 `CLAUDE.md` —— **工程文件夹一律创建**，非工程目录判断后跳过
- **检查当前目录是否已有** `start-claude.bat` / `CLAUDE.md`（**实际看目录文件**，别只信 CLAUDE.md 内容）。已有则跳过。
- **需要创建的场景（一律创建，含已有工程的文件夹）**：
  - **工程目录**（含 `.uvprojx`/`.sln`/`package.json`/`CMakeLists.txt` 等工程文件的文件夹，**即使已有完整代码**，如本机各种 STM32 工程、VS 工程、node 工程）→ 一律创建 `CLAUDE.md`（项目说明）和 `start-claude.bat`（启动文件）。
  - **空文件夹**（或近乎为空，像是准备从头做工程的地方）→ 判断用户要在此开始一个新工程，应创建 `CLAUDE.md` 和 `start-claude.bat`。
- **不需要创建的场景**：目录在**桌面 / C 盘或 D 盘根目录 / 文件很多的普通目录**（无工程文件、非工程性质的文件夹）→ 大概率用户只是要读取或修改某几个文件（单次需要 Claude 帮助），此时**不要创建** bat 和 md。
- 需要创建时，`start-claude.bat` 直接用已调试好的**最终模板**（不要重新摸索）：

```bat
@echo off
title Claude Code
set "WT=%LOCALAPPDATA%\Microsoft\WindowsApps\wt.exe"
set "DIR=%~dp0"
set "DIR=%DIR:~0,-1%"
if exist "%WT%" (
    start "" "%WT%" -d "%DIR%" cmd /k claude
) else (
    cd /d "%~dp0"
    claude
    pause
)
```

### 8. 使用场景判断 —— **根据工作目录判断当前用途**
- 在 Keil 工程文件夹（含 `.uvprojx`）→ 大概率 STM32 开发场景。
- 不在 Keil 工程文件夹 → 可能是学 C 语言、其他语言、或与编程无关；**不要硬把话题引到 STM32**，按用户实际问的内容作答。

### 9. 工程文件夹中先通读工程再回答（仅工程文件夹适用）
- **条件：仅当当前工作目录被识别为工程文件夹时执行**（含 `.uvprojx`/`.sln`/`package.json`/`CMakeLists.txt` 等工程文件，或用户明确表示正在做某项目）。
- 回答该工程相关问题时，**先完整通读用户编辑过的工程文件**（除只读库以外的全部源码 `.c/.h` 等），再下结论，**不靠直觉与经验**。
- 细节与背景见全局记忆 `read-project-before-answer.md`。

## 记忆位置说明

- **全局记忆**（按需召回）：`%USERPROFILE%\.claude\memory\`
- **项目记忆**（按目录隔离）：`%USERPROFILE%\.claude\projects\<项目名>\memory\`
- 本文件是**启动必加载**的全局入口，核心偏好在此；细节在 memory 目录按需召回。
